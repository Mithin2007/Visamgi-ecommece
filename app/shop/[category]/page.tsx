import {notFound} from "next/navigation"; import {db} from "@/server/db"; import {SiteHeader} from "@/components/site-header"; import {SiteFooter} from "@/components/site-footer"; import {ProductCard} from "@/components/product-card";
export default async function Category({params}:{params:Promise<{category:string}>}){const {category}=await params;const row=await db.category.findFirst({where:{slug:category,published:true},include:{products:{where:{published:true,archivedAt:null},include:{images:{orderBy:{sortOrder:"asc"}}}}}});if(!row)notFound();return <main><SiteHeader/><section className="shop shell"><div className="page-intro"><p className="eyebrow">Collection</p><h1>{row.name}</h1>{row.description&&<p className="page-intro__lede">{row.description}</p>}</div>{row.products.length?<div className="product-grid">{row.products.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="empty-state"><h2>Nothing here just yet.</h2><p>Explore the full collection to discover the pieces currently available.</p></div>}</section><SiteFooter/></main>}
