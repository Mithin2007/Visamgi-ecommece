import Link from "next/link"; import {db} from "@/server/db"; import {SiteHeader} from "@/components/site-header"; import {SiteFooter} from "@/components/site-footer"; import {ProductCard} from "@/components/product-card";
export default async function Shop(){const [products,categories]=await Promise.all([db.product.findMany({where:{published:true,archivedAt:null},include:{images:{orderBy:{sortOrder:"asc"}}},orderBy:{createdAt:"desc"}}),db.category.findMany({where:{published:true},orderBy:{sortOrder:"asc"}})]);return <main><SiteHeader/><section className="shop shell"><div className="page-intro"><p className="eyebrow">The collection</p><h1>Objects chosen for their soul.</h1><p className="page-intro__lede">A considered edit of Indian craft, enduring materials and quietly expressive forms.</p></div><div className="shop-links" aria-label="Browse collections"><Link className="is-active" href="/shop">All pieces</Link>{categories.map(c=><Link key={c.id} href={`/shop/${c.slug}`}>{c.name}</Link>)}</div>{products.length?<div className="product-grid">{products.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="empty-state"><h2>The collection is taking shape.</h2><p>No products are available yet. Return soon or explore our story.</p><Link className="button" href="/about">Our story</Link></div>}</section><SiteFooter/></main>}
