import { randomUUID } from "crypto"; import { requireCustomer } from "@/server/auth"; import { db } from "@/server/db"; import { cartTotal } from "@/server/cart"; import { placeOrder } from "./actions"; import { SiteHeader } from "@/components/site-header";
export default async function Checkout(){const user=await requireCustomer();const [addresses,cart,shipping]=await Promise.all([db.address.findMany({where:{userId:user.id},orderBy:{isDefault:"desc"}}),db.cart.findFirst({where:{userId:user.id,status:"ACTIVE"},include:{items:{include:{product:{include:{variants:true}}}}}}),db.shippingConfiguration.findFirst()]);const items=cart?.items??[],subtotal=cartTotal(items),shippingFee=shipping?.shippingCharge??0;if(!items.length)return <main><SiteHeader/><section className="shop shell"><h1>Your bag is empty.</h1></section></main>;return <main><SiteHeader/><section className="checkout shell"><p className="eyebrow">Checkout</p><h1>One final step.</h1>{addresses.length?<form action={placeOrder}><label>Delivery address<select name="addressId" required>{addresses.map(a=><option key={a.id} value={a.id}>{a.fullName} · {a.city} · {a.pincode}</option>)}</select></label><label>Payment method<select name="paymentMethod"><option value="RAZORPAY">Online payment — pending until Phase 5</option><option value="COD">Cash on delivery</option></select></label><input type="hidden" name="checkoutToken" value={randomUUID()}/><p>Subtotal ₹{subtotal.toString()}</p><p>Shipping ₹{shippingFee.toString()}</p><h2>Total ₹{subtotal.add(shippingFee).toString()}</h2><button className="button">Create order</button></form>:<p>Please add a saved address before placing an order.</p>}</section></main>}
