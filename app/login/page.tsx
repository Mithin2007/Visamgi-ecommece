"use client"; import {useActionState} from "react"; import Link from "next/link"; import {login} from "@/actions";
export default function Login(){const[s,a,p]=useActionState(login,{error:""});return <main className="customer-form shell"><p className="eyebrow">VISAMGI account</p><h1>Welcome back.</h1><form action={a}><label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" required/></label>{s.error&&<p role="alert">{s.error}</p>}<button className="button" disabled={p}>Sign in</button></form><p>New to VISAMGI? <Link href="/register">Create an account</Link></p></main>}
