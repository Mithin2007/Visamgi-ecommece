import Link from "next/link";
import { deleteAddress, saveAddress } from "@/actions";
import { requireCustomer } from "@/server/auth";
import { db } from "@/server/db";
import { SiteFooter } from "@/components/site-footer";
import { SiteHeader } from "@/components/site-header";
export default async function Addresses({searchParams}:{searchParams:Promise<{returnTo?:string}>}){const user=await requireCustomer(),{returnTo}=await searchParams,returnToCheckout=returnTo==="/checkout",addresses=await db.address.findMany({where:{userId:user.id},orderBy:{isDefault:"desc"}});return <main><SiteHeader/><section className="account-list shell"><p className="eyebrow">Your VISAMGI</p><div className="page-heading"><div><h1>Saved addresses</h1><p>Delivery details, ready when you are.</p></div>{returnToCheckout&&<Link className="text-link" href="/checkout">Return to checkout</Link>}</div>{returnToCheckout&&<div className="checkout-notice"><p>Add an address and you’ll return directly to delivery selection.</p></div>}{addresses.length?<div className="address-list">{addresses.map(address=><article className={`address-card${address.isDefault?" address-card--default":""}`} key={address.id}><div><p className="eyebrow">{address.isDefault?"Default address":"Saved address"}</p><h2>{address.fullName}</h2><p>{address.line1}{address.line2&&`, ${address.line2}`}<br/>{address.city}{address.district&&`, ${address.district}`}<br/>{address.state} — {address.pincode}</p><small>{address.phone}{address.type&&` · ${address.type}`}</small></div><form action={deleteAddress}><input name="id" type="hidden" value={address.id}/><button>Remove</button></form></article>)}</div>:<div className="empty-state empty-state--compact"><h2>No saved addresses yet.</h2><p>Add one below to make delivery selection effortless.</p></div>}<section className="address-form-section"><p className="eyebrow">Add an address</p><h2>Where should your pieces arrive?</h2><form className="address-form" action={saveAddress}><input type="hidden" name="returnTo" value={returnToCheckout?"/checkout":""}/><label>Full name<input name="fullName" autoComplete="name" required/></label><label>Phone<input name="phone" inputMode="tel" autoComplete="tel" required/></label><label className="address-form__wide">Address line<input name="line1" autoComplete="address-line1" required/></label><label className="address-form__wide">Address line 2<input name="line2" autoComplete="address-line2"/></label><label>City<input name="city" autoComplete="address-level2" required/></label><label>District<input name="district"/></label><label>State<input name="state" autoComplete="address-level1" required/></label><label>Pincode<input name="pincode" inputMode="numeric" autoComplete="postal-code" required/></label><label>Landmark<input name="landmark"/></label><label>Type<input name="type" placeholder="Home / Work"/></label><label className="form-check"><input name="isDefault" type="checkbox"/> Set as default delivery address</label><button className="button">Save address{returnToCheckout?" and return":""}</button></form></section></section><SiteFooter/></main>;}
