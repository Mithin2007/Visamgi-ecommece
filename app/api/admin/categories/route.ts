import { NextResponse } from "next/server"; import { currentAdmin } from "@/server/auth"; import { db } from "@/server/db"; import { categoryInput,recordAudit } from "@/server/admin";
export async function GET(){if(!(await currentAdmin()))return NextResponse.json({error:"Unauthorized"},{status:401});return NextResponse.json(await db.category.findMany({orderBy:{name:"asc"}}));}
export async function POST(req:Request){const admin=await currentAdmin();if(!admin)return NextResponse.json({error:"Unauthorized"},{status:401});try{const data=categoryInput.parse(await req.json());const row=await db.category.create({data});await recordAudit(admin.id,"CATEGORY_CREATED","Category",row.id);return NextResponse.json(row,{status:201});}catch{return NextResponse.json({error:"Invalid or conflicting category data."},{status:422});}}
