"use client"; import {useActionState} from "react"; import Link from "next/link"; import {register} from "@/actions";
export default function Register(){const[s,a,p]=useActionState(register,{error:""});return <main className="customer-form shell"><p className="eyebrow">Begin your collection</p><h1>Create an account.</h1><form action={a}><label>Name<input name="name" required/></label><label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" minLength={10} required/></label>{s.error&&<p role="alert">{s.error}</p>}<button className="button" disabled={p}>Create account</button></form><p>Already have an account? <Link href="/login">Sign in</Link></p></main>}
